$(function() {
  $('.header-content__bike').slick({
    autoplay: true,
    autoplaySpeed: 2000,
    arrows: false,
  });

  $('.gallery__wrapper').slick({
    autoplay: true,
    autoplaySpeed: 2000,
    arrows: false,
  });
});